import { createRoute } from "atomic-router";

export const routes = {
  home: createRoute(),
  resume: createRoute(),
  portfolio: createRoute(),
  growth: createRoute(),
  contacts: createRoute(),
};
